// Tailwind CSS v4 configuration
// Tailwind v4 uses a different approach with @import "tailwindcss" in CSS files
// This config provides minimal extensions

export default {
  // Minimal config for Tailwind v4 compatibility
};